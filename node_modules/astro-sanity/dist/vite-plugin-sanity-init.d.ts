import { ClientConfig } from "@sanity/client";
export declare function vitePluginSanityInit(config: ClientConfig): {
    name: string;
    resolveId(id: string): Promise<string | undefined>;
    load(id: string): Promise<string | undefined>;
};
